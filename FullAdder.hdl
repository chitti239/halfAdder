CHIP FullAdder{
    IN a, b, carryIn;
    OUT sum, carry;
  PARTS:
  Xor(a=a, b=b, out=xOut);
  And(a=a, b=b, out=yOut);
  Xor(a=xOut, b=carryIn ,out=sum);
  And(a=xOut , b= carryIn, out=nOut);
  Or(a=nOut, b=yOut, out=carry);
}